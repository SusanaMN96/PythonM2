hat_list = [1, 2, 3, 4, 5]  # This is an existing list of numbers hidden in the hat.

hat_list[2] = int(input("Enter an integer number to replace the middle number: "))# Step 1: write a line of code that prompts the user
# to replace the middle number with an integer number entered by the user.

del hat_list[4]# Step 2: write a line of code that removes the last element from the list.

print("This is the length of the existing list:", len(hat_list))# Step 3: write a line of code that prints the length of the existing list.

print(hat_list)
